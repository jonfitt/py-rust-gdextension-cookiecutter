# {{ cookiecutter.project_name_display }} GDExtension

Godot 4 addon for {{ cookiecutter.project_slug }}.

## Installation

1. Download `{{ cookiecutter.project_slug }}-<version>.zip` from your project's releases.
2. Extract the `{{ cookiecutter.project_slug }}` folder into your Godot project's `addons/` directory.
3. Open your project in Godot. The extension loads when Godot finds the `.gdextension` file.

## Requirements

- Godot {{ cookiecutter.godot_compatibility_minimum }} or later.
- Supported platforms: Linux (x86_64), Windows (x86_64), macOS (Apple Silicon).

## Demo

Download `{{ cookiecutter.project_slug }}-demo-<version>.zip`, extract anywhere, and open `project.godot` in Godot.

<!-- TODO: Link to your API documentation (docs/godot.md). -->
